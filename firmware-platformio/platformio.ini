[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
upload_speed = 921600

; BLEDevice, BLEServer, BLEUtils, and BLE2902 are included with the
; Espressif Arduino framework, so no extra lib_deps entry is needed.
